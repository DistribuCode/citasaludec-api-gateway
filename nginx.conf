server {
    listen 80;

    # ==========================
    # 🔐 AUTH MICROSERVICIOS
    # ==========================

    location /auth/ {
        proxy_pass http://52.7.96.46:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /user/ {
        proxy_pass http://52.7.96.46:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /registration/ {
        proxy_pass http://52.7.96.46:4001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ==========================
    # 📅 CITAS MICROSERVICIOS
    # ==========================

    location /appointments/ {
        proxy_pass http://52.207.101.135:4007;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /cancel/ {
        proxy_pass http://52.207.101.135:4008;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /availability/ {
        proxy_pass http://52.207.101.135:4010;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ==========================
    # 🔔 NOTIFICACIONES (pendientes)
    # ==========================

    # location /sms/ {
    #     proxy_pass http://<IP_DEL_SMS_SERVICE>:5000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }

    # location /email/ {
    #     proxy_pass http://<IP_DEL_EMAIL_SERVICE>:4003/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }

    # location /push/ {
    #     proxy_pass http://<IP_DEL_PUSH_SERVICE>:8080/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }

    # ==========================
    # 📋 LOGS
    # ==========================

    location /logs/ {
        proxy_pass http://54.145.51.77:4011;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ==========================
    # 🖥️ FRONTEND
    # ==========================

    location / {
        proxy_pass http://34.196.95.125:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        try_files $uri $uri/ /index.html;
    }
}
